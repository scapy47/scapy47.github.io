/*
import "p5"
import type p5 from "p5"
import Demo from "./Demo"

const Test = () => {
    function sketch() {
        setup = () => p5.createCanvas(600, 400, p5.WEBGL);

        draw = () => {
            background(250);
            normalMaterial();
            push();
            rotateZ(p5.frameCount * 0.01);
            rotateX(p5.frameCount * 0.01);
            rotateY(p5.frameCount * 0.01);
            plane(100);
            pop();
        };
    }
    return <Demo className="overflow-scroll rounded-3xl" />
}

export default Test
*/
